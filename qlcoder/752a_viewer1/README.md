命令行：

	grep "2015-08-24" uv.txt | cut -d " " -f 2 | sort | uniq | wc -l


`grep "2015-08-24" uv.txt` 筛选出 2015-08-24 这天的访问日志

`cut -d " " -f 2` 使用空格   来进行分割，并且取第二项

`sort` 排序，为下一步去重做准备

`uniq` 去重

`wc -l` 每行就是一个用户，按行统计

`|` 每个命令中间的竖线 | 是管道符号，见上一个命令的输出作为下一个命令的输入


参考自[D瓜哥](http://www.qlcoder.com/q/75f0)
